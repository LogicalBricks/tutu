%h1 Modificar datos del Lector
- if @lector.foto
  = image_tag @lector.foto_url, class: 'img-polaroid' 


= render 'form'

.btn-group
  = link_to 'Mostrar', @lector, class: 'btn'
  = link_to 'Foto', foto_lector_path(@lector), class: 'btn'
  = link_to 'Listado', lectores_path, class: 'btn'
