%h1 Listado de Lectores

- if @lectores.any?
  %table
    %thead
      %tr
        %th Nombre
        %th Primer apellido
        %th Segundo apellido
        %th Grado
        %th Grupo
        %th
        %th
        %th

    %tbody
      - @lectores.each do |lector|
        %tr
          %td= lector.nombre
          %td= lector.primer_apellido
          %td= lector.segundo_apellido
          %td= lector.grado
          %td= lector.grupo
          %td= link_to 'Mostrar', lector
          %td= link_to 'Editar', edit_lector_path(lector)
          %td= link_to 'Eliminar', lector, method: :delete, data: { confirm: '¿Estás seguro?' }
- else
  %div
    %h3 No se encontraron Lectores

%br

= link_to "Nuevo Lector", new_lector_path
